#include<stdio.h>
#include<conio.h>
int main()
{      char s[109],to,by;
int i;
	printf("enter the string,replace from,replace by");
	scanf("%s%c%c",&s,&to,&by);
//	   int i;
	for(i=0;s[i];i++)
	{
		if(s[i]==to)
		{
			s[i]=by;
		}

	     // printf("%c",s[i]);

	}
	printf("%c",s[i]);
	getch();
	return 0;
}
